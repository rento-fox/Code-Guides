// C++

#include <iostream>
#include <string>
using namespace std;

int main() {
    
    // for loop in C++
    for (int i = 5; i > 0; i--) {
        cout << to_string(i) + "\n"; // (prints numbers 5 to 1 vertically)
    }
    cout << "Liftoff!\n"; // Liftoff!
    
    return 0;
}

// EOF
